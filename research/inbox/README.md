# Research inbox

Files that the maintainer downloads by hand because the development sandbox
can only reach `github.com` and `pypi.org`. Drop them in this directory (or in
any GitHub repository or gist) and they become readable from the sandbox.

Keep originals unmodified. Add a line to the table below for every file so the
source stays traceable. Large binary datasets (hundreds of MB and up) do not
belong in git; put a download link and a checksum here instead.

| File | Origin URL | Why it is here | Added |
|---|---|---|---|
| _(none yet)_ | | | |

The prioritised wish list lives in `../sources.md` under **Wanted downloads**.
